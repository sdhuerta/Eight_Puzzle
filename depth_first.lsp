; Take in a puzzle into iterated depth first search
(defun idfs (puzzle)
	; iterate sequentially 
	(do* 

	; hold visited states
	(
		(curNode (make-node :state start :parent nil))
		(OPEN (list curNODE) )
		(CLOSED (nil) )
	)

	; Did we reach the goal state
	(())


	; hold expanded unvisited states


	; 

)